'use client'

import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#F3F3F3',
      secondary: '#b8b7ad',
    },
    secondary: {
      main: '#47E10C',
    },
    background: {
      default: '#252833',
      paper: '#373a47',
    },
  },
})
